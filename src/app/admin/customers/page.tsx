import { EmptyAdmin } from "../_tables";
export default function Page() {
  return <EmptyAdmin title="Customers" text="Customer accounts persist when DATABASE_URL is connected. Guest garage data stays on the device." />;
}
